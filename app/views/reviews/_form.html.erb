<%= form_with(model: @review, class: "shadow p-3 mb-3 bg-info rounded", local: true) do |f| %>

    # <div>
    #   <%= f.label :review %><br>
    #   <%= f.text_area :review %>
    # </div>

    <div>
      <%= f.submit %>
    </div>

<% end %>